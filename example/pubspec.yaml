name: bank_alfalah_payment_example
description: Demonstrates the bank_alfalah_payment SDK against a mock backend.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.6.0
  flutter: ">=3.22.0"

dependencies:
  bank_alfalah_payment:
    path: ../
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
